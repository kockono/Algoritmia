/**
 * Lo que tenemos que hacer es que ciere al mismo tiempo
 * https://leetcode.com/problems/valid-parentheses/
 * 20#
 */
function isValid(s: string): boolean {

    let parentesis:string[] = s.split("");
    
    return true;
    
};